;; --- 初始化加载路径 ---
(add-to-list 'load-path (expand-file-name "lisp" user-emacs-directory))

;; --- 基础包管理器设置 ---
(require 'package)
(setq package-archives '(("gnu"    . "https://elpa.gnu.org/packages/")
                         ("melpa"  . "https://mirrors.tuna.tsinghua.edu.cn/elpa/melpa/")
                         ("stable" . "https://mirrors.tuna.tsinghua.edu.cn/elpa/stable-melpa/")))

(package-initialize)

;; 自动安装 use-package（推荐加上 :ensure t 支持）
(unless (package-installed-p 'use-package)
  (package-refresh-contents)
  (package-install 'use-package))

(eval-when-compile
  (require 'use-package))

;; 确保 use-package 总是自动安装缺失的包
(setq use-package-always-ensure t)

;; --- 加载自定义模块 ---
(require 'setup-font)
(require 'setup-ui)
(require 'setup-edit)

(provide 'init)
;;; init.el --- My Emacs Configuration -*- lexical-binding: t; -*-

;; --- 加载路径 ---
(add-to-list 'load-path (expand-file-name "lisp" user-emacs-directory))

;; --- 包管理器设置（使用清华镜像，香港速度快）---
(require 'package)
(setq package-archives '(("gnu"    . "https://elpa.gnu.org/packages/")
                         ("melpa"  . "https://mirrors.tuna.tsinghua.edu.cn/elpa/melpa/")
                         ("stable" . "https://mirrors.tuna.tsinghua.edu.cn/elpa/stable-melpa/")))

(package-initialize)

;; 安装 use-package
(unless (package-installed-p 'use-package)
  (package-refresh-contents)
  (package-install 'use-package))

(eval-when-compile (require 'use-package))
(setq use-package-always-ensure t)

;; --- 加载自定义模块（字体必须最先加载）---
(require 'setup-font)
(require 'setup-ui)
(require 'setup-edit)

(provide 'init)
